version: '2'
services:
    opengive-elasticsearch:
        image: elasticsearch:2.4.1
        # volumes:
        #     - ~/volumes/opengive/elasticsearch/:/usr/share/elasticsearch/data/
        ports:
            - 9200:9200
            - 9300:9300
